# AWS 네트워킹 초보 가이드 — VPC / 서브넷 / 라우팅 / 보안그룹 | 함지용

> 대상: 온프렘 네트워크 조닝·VLAN·방화벽 경험은 있으나 AWS 네트워킹은 처음.
> 전략: **온프렘 개념 → AWS 용어로 갈아타기.** 이미 아는 걸 새 이름표로 바꿔 붙이는 방식이라 빠릅니다. SAA 시험과 EKS 실습에 그대로 쓰입니다.

---

## 0. 온프렘 → AWS 한 장 대응표 (먼저 이것부터)

| 온프렘/가상화에서 하던 것             | AWS 대응                    | 한 줄 요약                         |
| -------------------------- | ------------------------- | ------------------------------ |
| IDC 안의 우리 회사 전용 네트워크       | **VPC**                   | 내 전용 가상 네트워크(격리된 IP 대역)        |
| VLAN / 네트워크 존(zone) 분리     | **서브넷(Subnet)**           | VPC를 잘게 나눈 IP 구획, AZ(가용영역)에 귀속 |
| L3 스위치 / 라우팅 테이블           | **라우트 테이블(Route Table)**  | "이 대역으로 갈 땐 어디로" 규칙            |
| 인터넷 회선 / 게이트웨이             | **인터넷 게이트웨이(IGW)**        | VPC ↔ 인터넷 출입구                  |
| 내부망 서버가 외부로만 나갈 때(프록시/NAT) | **NAT 게이트웨이**             | 프라이빗 서브넷의 아웃바운드 전용 통로          |
| 방화벽 정책(서버 단위)              | **보안 그룹(Security Group)** | 인스턴스/ENI 단위 방화벽, 허용 규칙만        |
| 방화벽 정책(네트워크 존 단위)          | **NACL**                  | 서브넷 단위 방화벽, 허용+거부              |
| 조닝(보안 존 간 통신 통제)           | **서브넷 + 라우팅 + SG 조합**     | 이게 AWS식 조닝                     |

> 💡 면접 카드: "온프렘에서 하던 조닝(VLAN·존 분리·방화벽 정책)이 AWS에선 VPC·서브넷·라우트테이블·보안그룹의 조합으로 표현된다 — 계층은 같고 이름과 관리 방식이 코드화(IaC)됐을 뿐."

---

## 1. VPC — 내 전용 가상 네트워크

- **정의**: 리전 안에 만드는 격리된 사설 네트워크. 온프렘의 "우리 회사 네트워크 대역"과 같음.
- **CIDR**: VPC를 만들 때 IP 대역을 정함. 예 `10.0.0.0/16` (= 10.0.0.0 ~ 10.0.255.255, 약 6.5만 개). 사설 대역(10/8, 172.16/12, 192.168/16) 사용.
- **범위**: VPC는 **리전(Region)** 단위. 그 안의 서브넷은 **AZ(가용영역)** 단위.

```
Region: ap-northeast-2 (서울)
┌─────────────────────────── VPC (10.0.0.0/16) ───────────────────────────┐
│                                                                          │
│   AZ-a                              AZ-c                                  │
│  ┌───────────────┐                ┌───────────────┐                      │
│  │ 서브넷들...     │                │ 서브넷들...     │                      │
│  └───────────────┘                └───────────────┘                      │
└──────────────────────────────────────────────────────────────────────────┘
```

> CIDR 감각: `/16`은 뒤 16비트가 host → 서브넷용으로 쪼갬. `/24`면 256개(실사용 251개). 온프렘 서브넷팅 경험 그대로 적용됩니다.

---

## 2. 서브넷 — VPC를 나눈 구획 (Public vs Private)

- **정의**: VPC CIDR을 더 잘게 나눈 IP 블록. **하나의 서브넷 = 하나의 AZ**에 존재.
- **핵심 개념 — Public이냐 Private이냐는 "라우팅으로 결정"됨** (서브넷에 무슨 스위치가 붙었냐가 아님):
  - **퍼블릭 서브넷** = 라우트 테이블에 `0.0.0.0/0 → IGW(인터넷 게이트웨이)` 경로가 있는 서브넷. 인터넷과 직접 통신 가능.
  - **프라이빗 서브넷** = 그 경로가 없는 서브넷. 인터넷에서 직접 못 들어옴. 나갈 땐 NAT 경유.
- **AWS가 예약하는 IP**: 각 서브넷에서 **처음 4개 + 마지막 1개(총 5개)**는 AWS가 예약(네트워크주소, VPC 라우터, DNS, 예약, 브로드캐스트). `/24`면 실사용 251개.

**전형적 3티어 배치 (SAA 단골 그림):**
```
VPC 10.0.0.0/16
├── 퍼블릭 서브넷  10.0.1.0/24 (AZ-a)   ← ALB, NAT GW, Bastion
├── 퍼블릭 서브넷  10.0.2.0/24 (AZ-c)
├── 프라이빗 서브넷 10.0.11.0/24 (AZ-a)  ← 앱 서버 / EKS 노드
├── 프라이빗 서브넷 10.0.12.0/24 (AZ-c)
├── DB 서브넷      10.0.21.0/24 (AZ-a)  ← RDS
└── DB 서브넷      10.0.22.0/24 (AZ-c)
```
> 온프렘 대응: 퍼블릭 = DMZ 존, 프라이빗 = 내부 서비스 존, DB 서브넷 = 데이터 존. 조닝과 완전히 동형.

---

## 3. 라우팅 — "어디로 보낼지" 규칙

- **라우트 테이블(Route Table)**: 목적지 대역별로 다음 홉을 지정. 서브넷마다 하나의 라우트 테이블이 연결됨.
- **local 경로**: 모든 라우트 테이블에 자동 존재하는 `10.0.0.0/16 → local`. **VPC 내부끼리는 항상 통신 가능**(삭제 불가). 이게 있어서 서브넷 간 통신이 기본으로 됨.
- **인터넷 게이트웨이(IGW)**: VPC에 1개 부착. 퍼블릭 IP를 가진 리소스의 인터넷 입출입을 담당(사실상 NAT 역할도).
- **NAT 게이트웨이**: **퍼블릭 서브넷에** 두고 EIP(고정 공인IP)를 붙임. **프라이빗 서브넷의 아웃바운드**(예: 패키지 다운로드, API 호출)를 대신 내보냄. 인바운드는 안 됨. **유료(시간+데이터)라 비용 주의.**

**라우트 테이블 예시:**
```
[퍼블릭 서브넷용 RT]
  10.0.0.0/16   → local        (VPC 내부)
  0.0.0.0/0     → igw-xxxx      (인터넷 직통)

[프라이빗 서브넷용 RT]
  10.0.0.0/16   → local
  0.0.0.0/0     → nat-xxxx      (인터넷은 NAT 통해서만 나감)
```

**트래픽 흐름 이해 (핵심):**
- 외부 사용자 → ALB(퍼블릭 서브넷) → 앱 파드(프라이빗 서브넷): 인바운드는 퍼블릭의 LB가 받아서 내부로 프록시.
- 앱 파드(프라이빗) → 외부 API: `0.0.0.0/0 → NAT` 타고 나감. 외부에서 파드로 직접 진입은 불가(그래서 안전).

---

## 4. 보안 그룹(SG) vs NACL — 가장 자주 틀리는 부분

두 개 다 방화벽인데 **계층과 성격이 다릅니다.** SAA 시험 단골, 실무 필수.

| 구분 | 보안 그룹 (Security Group) | 네트워크 ACL (NACL) |
|---|---|---|
| 적용 단위 | **인스턴스/ENI**(서버 단위) | **서브넷**(존 단위) |
| 상태 | **Stateful(상태 유지)** | **Stateless(상태 없음)** |
| 규칙 | **허용(allow)만** | 허용 + **거부(deny)** 둘 다 |
| 리턴 트래픽 | 자동 허용(요청 나가면 응답 자동 통과) | **수동**: 인바운드/아웃바운드 각각 규칙 필요 |
| 평가 | 모든 규칙 종합 | 번호 순서대로(낮은 번호 우선) |
| 기본값 | 인바운드 차단 / 아웃바운드 전체 허용 | 기본 NACL은 전체 허용 |
| 특이점 | **다른 SG를 소스로 참조 가능**(예: "ALB의 SG에서 오는 것만 허용") | IP/포트로만 |

**Stateful vs Stateless가 핵심 — 예시로 이해:**
- **SG(Stateful)**: 인바운드에 "80 허용"만 넣으면, 그 요청의 **응답은 자동으로 나감**. 아웃바운드 규칙 신경 안 써도 됨.
- **NACL(Stateless)**: 인바운드 80을 허용해도, 응답이 나가려면 **아웃바운드에 임시 포트(ephemeral, 보통 1024~65535) 허용**을 따로 넣어야 함. 이걸 빼먹어서 "접속은 되는데 응답이 안 온다"가 초보 단골 실수.

**실무 권장**: 보통 **SG로 세밀하게 통제**하고, NACL은 서브넷 단위 광역 차단(예: 특정 IP 대역 blackhole)에만 보조적으로. 온프렘으로 치면 SG = 서버 방화벽, NACL = 존 경계 방화벽.

**SG 참조 패턴 (매우 중요·실무 필수):**
```
ALB의 SG:   인바운드 443 from 0.0.0.0/0 (인터넷)
앱의 SG:    인바운드 8080 from [ALB의 SG]   ← IP가 아니라 "ALB SG에서 오는 것만"
RDS의 SG:   인바운드 3306 from [앱의 SG]     ← 앱에서 오는 것만
```
→ 이렇게 SG를 체인으로 걸면 IP 하드코딩 없이 계층 격리. 온프렘 조닝의 "존 간 통신 정책"과 동일한 사고.

---

## 5. 미니 실습 (직접 손으로 — 30~60분, 거의 무료)

> EC2 t2.micro는 프리티어. NAT GW는 유료이니 실습 후 삭제. 리전은 서울 고정.

**랩 A — VPC 수동 구축 (콘솔로 개념 체득)**
1. VPC 생성: `10.0.0.0/16`
2. 서브넷 2개: 퍼블릭 `10.0.1.0/24`(AZ-a), 프라이빗 `10.0.11.0/24`(AZ-a)
3. IGW 생성 → VPC에 attach
4. 퍼블릭용 라우트테이블: `0.0.0.0/0 → IGW` 추가 → 퍼블릭 서브넷에 연결
5. NAT GW: 퍼블릭 서브넷에 생성(EIP 할당) → 프라이빗용 라우트테이블 `0.0.0.0/0 → NAT` → 프라이빗 서브넷에 연결
6. 퍼블릭 서브넷에 EC2(퍼블릭IP on, SG 인바운드 22 from 내 IP), 프라이빗 서브넷에 EC2(퍼블릭IP off)
7. **검증**: 내 PC → SSH → 퍼블릭 EC2 접속 성공 / 퍼블릭 EC2 → 프라이빗 EC2 내부 SSH(10.0.11.x) 성공 / 프라이빗 EC2에서 `curl https://google.com` (NAT 통해 나감) 성공 / 내 PC → 프라이빗 EC2 직접 접속은 **실패**(정상)
8. **삭제**: NAT GW → EIP 해제 → 나머지 정리

**랩 B — SG 체인 실습**
- 퍼블릭 EC2 SG: 22 from 내IP
- 프라이빗 EC2 SG: 22 from [퍼블릭 EC2의 SG]  ← IP 아닌 SG 참조
- 검증: 퍼블릭에서만 프라이빗으로 SSH 되고, 다른 데선 안 됨

**랩 C — NACL로 stateless 체감(선택)**
- 프라이빗 서브넷 NACL에서 아웃바운드 ephemeral(1024-65535)을 일부러 막아보고 "인바운드는 허용인데 응답이 안 오는" 현상 재현 → 다시 열어 복구. (Stateless가 몸에 박힘)

---

## 6. EKS와의 연결 (실습 플레이북과 이어짐)

- EKS 노드는 보통 **프라이빗 서브넷**에 둠(보안). 아웃바운드는 NAT로.
- **서브넷 태그 (초보 단골 함정)**: AWS Load Balancer Controller가 서브넷을 자동 인식하려면 태그 필요.
  - 퍼블릭 서브넷: `kubernetes.io/role/elb = 1` (인터넷 향 ALB/NLB용)
  - 프라이빗 서브넷: `kubernetes.io/role/internal-elb = 1` (내부용)
  - (클러스터 소유 태그: `kubernetes.io/cluster/<클러스터명> = shared` 또는 `owned`)
- 이 태그를 빼먹으면 "Ingress 만들었는데 ALB가 안 생김" → **면접에서 쓸 좋은 트러블슈팅 소재**.

---

## 7. 자주 틀리는 것 (Gotchas) & SAA 포인트

- ❌ "퍼블릭 서브넷에 뒀는데 인터넷이 안 돼요" → 라우트테이블에 IGW 경로 없거나, 인스턴스에 퍼블릭 IP 미할당, 또는 SG 인바운드 막힘. **3개(RT/퍼블릭IP/SG)를 순서대로 점검.**
- ❌ "프라이빗 인스턴스가 yum/apt가 안 돼요" → NAT GW 없거나 프라이빗 RT에 NAT 경로 없음.
- ❌ "SG는 열었는데 통신이 안 돼요" → NACL이 막고 있거나, NACL stateless라 리턴 포트가 막힘.
- ❌ NAT GW를 안 지워서 요금 발생 → 실습 후 **반드시 삭제**(EIP도).
- 📌 SAA 빈출: SG(stateful) vs NACL(stateless) 차이, 퍼블릭/프라이빗 서브넷 정의(라우팅 기준), NAT GW vs NAT Instance, VPC Peering/TGW/PrivateLink 개념, VPC Endpoint(S3 등 AWS 서비스에 사설 접속).
- 📌 지용님 우대 연결: **TGW(Transit Gateway), PrivateLink**는 아정당·한국신용데이터 우대라 개념 확실히. VPC Endpoint는 "인터넷 안 타고 S3/ECR 접근" — EKS 이미지 풀에도 쓰임.

---

## 8. 체크리스트

**개념**
- [ ] VPC/서브넷/AZ 관계  - [ ] 퍼블릭 vs 프라이빗(라우팅 기준)  - [ ] IGW vs NAT GW  - [ ] local 경로  - [ ] SG(stateful) vs NACL(stateless)  - [ ] SG 참조 체인  - [ ] VPC Endpoint  - [ ] Peering/TGW/PrivateLink 개념

**실습**
- [ ] 랩A VPC 수동 구축·검증  - [ ] 랩B SG 체인  - [ ] 랩C NACL stateless 체감  - [ ] EKS 서브넷 태그 이해  - [ ] 전부 삭제 확인

**면접 자산**
- [ ] "온프렘 조닝 = VPC/서브넷/SG" 대응 서사 정리  - [ ] 실습 중 겪은 에러 1~2개를 트러블슈팅 노트로

---

> 다음 단계: 이 가이드로 네트워킹 감각이 잡히면 → SAA 강의의 VPC 챕터는 복습 수준으로 빠르게 넘어가고 → 플레이북 EKS 랩 1(클러스터 생성)로 이어집니다. 온프렘 네트워크 실무가 있어서 이 영역은 지용님이 가장 빨리 통과할 파트예요.
